import { OpaqueToken } from '@angular/core';

export const BASE_URL = new OpaqueToken('BASE_URL');
export const API_VERSION = new OpaqueToken('API_VERSION');
export const PUBLIC_KEY = new OpaqueToken('PUBLIC_KEY');
